pipeline {
	agent none
	options { skipDefaultCheckout(true) }

	stages {
		agent any
		
		stage('Docker build') {
			steps {
				sh 'docker build -t frontend:latest /home/ubuntu/S05P13D103/frontend'
				sh 'docker build -t backend:latest /home/ubuntu/S05P13D103/backend'
			}
		}
	}
}